function Carousel({pictures}) {
  


  return (
    <div className="carousel">
      {/* {pictures.map((slide, index) => (
        <img src={slide} alt={`carousel-${index}`} key={`carousel-${index}`} className="carousel__image"/>
      ))} */}Ceci est un carousel
    </div>
  );
}

export default Carousel;
